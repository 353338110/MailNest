# Gmail 真实账号端到端验收记录

> 使用本模板记录 [`gmail-e2e.md`](gmail-e2e.md) 的执行结果。请使用专用测试账号，且**绝不**记录 access token、refresh token、授权码、client secret、完整请求体、邮件正文或附件内容。

## 执行信息

| 字段 | 填写内容 |
|---|---|
| 验收日期（UTC） | `<YYYY-MM-DD>` |
| 应用版本 / Git 提交 | `<version> / <commit>` |
| Flutter 版本 | `3.44.4` |
| 运行平台与版本 | `<macOS / Windows / Android / iOS>` |
| OAuth Client 类型 | `<Desktop / iOS / Android>` |
| 测试账号标识 | `<脱敏邮箱，例如 g***@example.com>` |
| 执行人 | `<姓名或团队代号>` |

## 前置检查

| 检查项 | 结果 | 备注（仅记录脱敏信息） |
|---|---|---|
| 使用专用测试账号 | `通过 / 不通过` | |
| OAuth 回调配置可用 | `通过 / 不通过` | |
| 测试邮件和自定义 Label 已准备 | `通过 / 不通过` | |
| 日志、截图和数据库不含凭证明文 | `通过 / 不通过` | |

## 核心功能验收

| 编号 | 场景 | 预期结果 | 实际结果 | 状态 |
|---:|---|---|---|---|
| 1 | 首次 OAuth 授权 | 浏览器回调后账号可保存，SQLite 仅保存 token 引用 | | `通过 / 失败 / 阻塞` |
| 2 | 文件夹与邮件同步 | Label 映射为文件夹，邮件列表有正确账号标识 | | `通过 / 失败 / 阻塞` |
| 3 | 邮件详情与附件 | HTML/纯文本正文可读，附件信息和下载正常 | | `通过 / 失败 / 阻塞` |
| 4 | 发信与已发送邮件 | 收件账号收到邮件，已发送邮件可见 | | `通过 / 失败 / 阻塞` |
| 5 | 远端草稿 | 新建、更新、删除草稿与 Gmail 状态一致 | | `通过 / 失败 / 阻塞` |
| 6 | 邮件状态操作 | 已读、星标、移动 Label、删除后本地与网页端一致 | | `通过 / 失败 / 阻塞` |

## OAuth 生命周期验收

| 编号 | 场景 | 预期结果 | 实际结果 | 状态 |
|---:|---|---|---|---|
| 1 | access token 过期 | 使用有效 refresh token 自动恢复同步 | | `通过 / 失败 / 阻塞` |
| 2 | 首次请求 401 | 强制刷新一次并仅重试原请求一次 | | `通过 / 失败 / 阻塞` |
| 3 | refresh token 缺失/失效 | 给出明确“重新连接 Gmail”操作 | | `通过 / 失败 / 阻塞` |
| 4 | 重新授权账号不一致 | 拒绝保存并明确说明原因 | | `通过 / 失败 / 阻塞` |
| 5 | 异常信息脱敏 | UI、日志和本地数据库不泄露凭证或完整请求体 | | `通过 / 失败 / 阻塞` |

## 问题与结论

| 编号 | 严重级别 | 现象 | 最小复现步骤 | 脱敏证据编号 | 处理状态 |
|---:|---|---|---|---|---|
| 1 | `P0 / P1 / P2` | | | | |

**最终结论：** `通过 / 有条件通过 / 不通过`

**后续动作：** `<链接至 issue、PR 或下一次验收记录>`
